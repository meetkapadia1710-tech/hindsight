httpx>=0.27
psutil>=5.9
pywin32>=306; sys_platform == "win32"
fastapi>=0.111
uvicorn>=0.30

# Optional — for the Win+H system-tray overlay (hindsight.overlay):
#   pip install pystray keyboard Pillow
# Optional — for screenshot-OCR (config ocr = true):
#   pip install Pillow winrt-Windows.Media.Ocr winrt-Windows.Graphics.Imaging \
#     winrt-Windows.Storage.Streams winrt-Windows.Globalization \
#     winrt-Windows.Foundation winrt-Windows.Foundation.Collections
